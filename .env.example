# Nhost Configuration
# These environment variables are required for the application to work with Nhost

# Nhost Project Configuration
# Get these values from your Nhost project dashboard at https://app.nhost.io
REACT_APP_NHOST_SUBDOMAIN=your-project-subdomain
REACT_APP_NHOST_REGION=your-project-region

# Example values:
# REACT_APP_NHOST_SUBDOMAIN=buzzconnect-abc123
# REACT_APP_NHOST_REGION=us-east-1

# Note: 
# - Replace 'your-project-subdomain' with your actual Nhost project subdomain
# - Replace 'your-project-region' with your actual Nhost project region
# - These values can be found in your Nhost project settings
# - For local development, you can use 'local' and 'localhost' respectively
